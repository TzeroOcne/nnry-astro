// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://tzeroocne.github.io',
  base: '/nnry-astro',
  server: {
    port: 7673,
  },
});
